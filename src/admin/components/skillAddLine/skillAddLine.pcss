.skill-add-line-component {
  display: flex;
  align-items: center;
}

.title {
  flex: 1;
}

.percent {
  margin: 0 30px 0 10px;
  position: relative;

  &:after {
    content: "%";
    position: absolute;
    top: 9px;
    right: 0;
  }
}
